# Backend TODO (не-монографы) — сводка для бэкенд-агента

**От:** мобильный агент «Что растёт». **Дата:** 2026-06-18.
Что от бэкенда ещё ждёт клиент, КРОМЕ дистилляции монографов (та — в
`HANDOFF-monograph-harvest-*` и `HANDOFF-monograph-quality-*`). По приоритету.

## Уже сделано (НЕ трогать, для контекста)
device_key в `/identify` ✅ · `/quests/places/near` + `/quests/place/{id}/set` ✅ ·
`/quests/leaderboard` ✅ · **уровневые значки (tiers) + очки ✅** (клиент подключил,
TestFlight 1.1 ушёл).

## P0 — фундамент (гейтит точность квестов)
1. **Чистка данных Слоя 1** (идентичность: латынь/имена/merge) + освобождение **iNat
   rate-limit** (общий на чистку/walk/set-builder). Гейтит:
   - **чистый пересчёт наборов значков** — текущие наборы построены на НЕсмёрженном
     списке видов → дубль-шум (один вид = 2 карточки в `place/set`). После merge
     перегнать `QuestSetBuilderWorkflow` по всем местам.
   - массовый монограф (это «монографы», здесь только как зависимость).

## P1 — покрытие (накопительное, иначе фичи «пустые»)
2. **Наборы значков по окнам.** Посчитано ТОЛЬКО `second-half-06` (333 места Москвы).
   Нужны прогоны `QuestSetBuilderWorkflow(window)` для следующих полумесячных окон по мере
   смены сезона (`first-half-07`, `second-half-07`, …) — иначе в новом окне `places/near`
   вернёт места без наборов и значки будут недоступны. Желательно автоматизировать
   (Temporal cron на смену окна).
3. **Биотопы — покрытие.** Чипы «Где растёт» (`?view=field.habitat`) живые, но биотоп
   назначен ПОДМНОЖЕСТВУ растений (у эталонного Хмеля `habitat=null`). Проставить биотопы
   большинству карточек. Плюс `habitat.regions` сейчас сырой длинный текст из источников —
   нормализовать в короткую географию (как договаривались).

## P2 — будущее (отдельные этапы, не начато)
4. **Квест↔биотоп**: «биотоп → найди рядом в этом месте». Нужна половина (b) биотоп-домена:
   **GPS→биотоп через OSM landcover** — предрасчёт био-зон для `quest_places` (какие биотопы
   есть в каждом месте), чтобы связать «определи виды биотопа X здесь». Клиент уже на OSM,
   шлёт GPS; не хватает landcover-слоя на бэке.

## Можно СНЯТЬ с бэкенда
5. **Пагинация `/walk`** («ещё рядом», была в `PLAN-quests-progression` ч.1) — мы ушли на
   квесты-вокруг-МЕСТ (`places/near` + `place/set`), live-walk демонтирован с критического
   пути. Пагинация walk больше **не нужна** — не делайте.

## Связанные доки
`HANDOFF-quests-places-2026-06-17` (place-эндпоинты) · `HANDOFF-gamification-tiers-2026-06-17`
(tiers/points, сделано) · `PLAN-quests-progression` (контекст). Монографы — отдельно.
