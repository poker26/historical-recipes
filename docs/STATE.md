# STATE — карта проекта (источник правды)

_Обновлено: 2026-06-16. Цель проекта: база знаний рус. травничества/рецептов/веществ для будущих
AI-агентов + клиент «Что растёт» (фото→вид→монограф). Два слоя: **Слой 1 = чистые ДАННЫЕ → Слой 2 =
предрасчётный читательский монограф**._

---

## 🟢 СЕЙЧАС РАБОТАЕТ САМО (не трогать)
- **Корпус** (`book-dispatcher`, пул=3): домолачивает хвост **~10 книг** (большие атласы). Резюм-фикс
  растений работает — книги доходят надёжно, без циклов. → когда сольётся, открывается тяжёлая цепочка.
- Прод: backend/worker/dispatcher up. Деплой = scp→`docker compose build`→`up`. **НЕ терминировать
  книги вручную** (пересоздаёт workflow как attempt-1 → delete → переделка; резюм только на внутр. ретраях).

## 🔴 ОСТАНОВЛЕНО (ждёт своего часа)
- **iNat-обогащение** (`plant-cleanup` терминирован) — 429-штормило, гасило walk. Незавершено: **~6691
  растение с латынью без фото**.
- **Квест set-builder** (`QuestSetBuilderWorkflow`) — построен, не запущен (iNat-контеншн). 333 места Москвы готовы.
- **Масс-монограф** (`ReaderMonographWorkflow`) — построен, не запущен (гейт = ВЕСЬ Слой-1).

---

## СПИНА ЗАВИСИМОСТЕЙ (почему такой порядок)
```
корпус слить ──► финальный backfill латыни (394+ новых карт) ──► merge/relink (ДЕСТРУКТИВ)
     │                                                                      │
     └──► iNat-фото (6691) ──► масс-монограф (Слой-2) ◄───────── вся чистка Слоя-1
                                      ▲
LLM-адъюдикации (review-residue, #3 иерархия, over_merge) ── делят LLM с корпусом
```
- **Корпус — вход.** Пока добавляет карты — backfill/merge гоняются за движущейся целью. Дослить ПЕРВЫМ.
- **merge/relink ДЕСТРУКТИВНЫ** — только после слива + после чистки alias.collision (перезахватит).
- **Слой-2 гейтится ВСЕМ Слоем-1** (генерить из грязи = полировать мусор).

---

## ДОМЕНЫ — статус

### Растения (Слой-1, фундамент)
- ✅ OCR latin/name репэйр; ✅ latin-backfill (главный проход: ~430 авто + 4927 review-residue).
- ⏳ **394 новых латынь-less карт** (от корпуса) — добор backfill ПОСЛЕ слива.
- ⏳ 4927 review-residue (auto-gate не решил ~91%) → **LLM-адъюдикатор** (отложен).
- ⏳ merge дублей/фрагментов (1f), alias.collision магниты (1g) — деструктив, ждёт.

### Вещества (нормализатор) — 2.5 из 3
- ✅ **#1 дедуп номенклатуры** (химия-канон, α/β-изомеры целы) — применён, 6604→6055.
- ✅ **#2 не-целевые** (ферменты/синтетика/гормоны подавлены в показе; витамины/минералы оставлены).
- ✅ **#3 иерархия — детектор** (`compound.bad_hierarchy`, 109 кандидатов). ⏳ LLM-адъюдикация отложена.
- 🔜 **зашить дедуп+фильтр в reference-пайплайн** (чтобы добор не плодил мусор) — В РАБОТЕ.

### Рецепты (Слой-1) — ждёт растений
- 48869 рецептов, все grounded. Дефекты: ~1560 дублей, монографии-как-рецепт (127), relink. Ждёт 1f/1g.

### Слой-2 монограф — ПОСТРОЕН, гейтится
- Пайплайн (distill→qwen→publish-gate→хранение) + отдача `?view=field` + review `/api/monographs`. 2 опубл.,
  2 P0-pending. Масс-прогон ждёт Слоя-1.

### Квесты — бэкенд ГОТОВ (Фазы 1-6)
- Прогулки на клиенте; бейджи ждут set-builder (iNat). Контракт отдан клиенту.

### Линтер качества — 12 валидаторов
- Детекция структурного вычерпана. Резолюция (адъюдикация/деструктив) ждёт LLM/корпус.

---

## РЕФЕРЕНСЫ (добор литературы)
- ✅ 17 справочников залиты (MinIO, `status=wishlist`, припарковано). Все PDF.
- ✅ Пилот Волынца прошёл → вскрыл, что нужен нормализатор (сделан #1/#2/#3-детектор).
- 6 из 7 химкниг — СКАНЫ (OCR-формулы поедут). Прогон — после слива корпуса + чистого пайплайна.

---

## ОЧЕРЕДЬ ДЕЙСТВИЙ

### Можно СЕЙЧАС (не блокируется, не конфликтует)
1. 🔜 **Зашить нормализатор в reference-пайплайн** (код) — В РАБОТЕ.
2. **#3 LLM-адъюдикация** 109 кандидатов (ограниченный LLM) → null containment → перезапуск dedup.
3. Sources-дедуп (фронт) + чек «противопоказание→ложн.токсичность» (Малина-класс, ~106). _Чипы созданы._
4. Перегенерить 2 P0-монографа (упор на примирение вердикта).

### После СЛИВА КОРПУСА (разом, тяжёлая цепочка)
backfill-добор латыни (394+) → iNat-фото (6691) → merge/relink → recipe-чистка → масс-монограф → квест-set-builder.

### Открытые задачи-чипы
- `task_283be0e6` — противопоказание→ложная токсичность (малина и ~106).
- `task_e7937376` — hardening рецепт/секция (УЖЕ в коде, чип избыточен).

---

## ГРАБЛИ (уроки сессии — не повторять)
- **idle-tx**: не держать DB-транзакцию во время LLM/embed/HTTP — коммитить ДО вызова (90-мин таймаут = band-aid).
- **Резюм**: per-book активность ОБЯЗАНА резюмиться (delete только attempt-1 + skip-existing), иначе цикл сутками.
- **iNat** — дефицит ~60/мин: фон (backfill/set-builder) штормит и гасит live walk → не гонять вместе.
- **Дедуп веществ**: канон ДОЛЖЕН беречь α/β/индексы (иначе слил бы α-пинен с β-пиненом).
- **«Грубый» детектор опасен**: проверять на реальных данных (self-parent/trivial-parent оказались валидны).
- **Не терминировать книги вручную** + деплой через scp (git догнали коммитом 2026-06-16).
