# Dependencies
node_modules/
__pycache__/
*.pyc
.venv/
venv/

# Environment
.env
.env.local

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Build
.next/
dist/
build/
*.egg-info/

# Logs
*.log

# Docker volumes (local)
data/

# Local MCP client config (points at a personal SSH tunnel)
.mcp.json

# Claude Code local session/config
.claude/

# Scratch / one-off dumps from admin & dedup runs
/*.json
/backend/_test_recipe.txt
/cnt.txt

# Android/KMP client lives in its own repo (chto-rastet-android) — never vendor it
# here (158MB + signing keystore). It landed in the working tree by accident.
android/
